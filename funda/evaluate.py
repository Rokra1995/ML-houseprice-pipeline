import matplotlib.pyplot as plt

class ModelEvaluator(object):

    def __init__(self, base_folder):
        self.base_folder = base_folder

    def evaluate_neural_network(NN):

        return print('Model evaluated. Plots can be found in the plots folder')
    
    def evaluate_random_forest(RF):

        return print('Model evaluated. Plots can be found in the plots folder')